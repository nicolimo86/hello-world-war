---

  - name: "create zip archive"
    hosts: localhost
    gather_facts: false
    tasks:
      - name: "create archive"
        archive:
          path: 
            - ../dist/hello-world.war
            - ../dist/appConfig
          dest: ./artifact.zip
          format: zip
      